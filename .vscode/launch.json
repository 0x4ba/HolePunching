{
    "configurations": [
    {
        "name": "Launch Package",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "args": ["-s"],
        "program": "${workspaceFolder}"
    }
    ]
}